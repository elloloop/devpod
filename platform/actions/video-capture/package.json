{
  "name": "@devpod/video-capture",
  "version": "0.1.0",
  "private": true,
  "description": "Record video demos and capture screenshots of web and mobile apps",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "postinstall": "npx playwright install chromium 2>/dev/null || echo 'Playwright browser install skipped — run npx playwright install chromium manually'"
  },
  "dependencies": {
    "playwright": "^1.45.0",
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
